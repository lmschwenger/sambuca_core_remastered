# SAMBUCA Core Remastered - Requirements
# Semi-Analytical Model for Bathymetry, Un-mixing, and Concentration Assessment

# Core scientific computing dependencies
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0

# Plotting and visualization (required for GUI)
matplotlib>=3.5.0

# Optional dependencies that may be needed for full functionality:
# Uncomment the following lines if you need these features:

# For satellite image I/O and geospatial operations:
# rasterio>=1.2.0
# gdal  # Note: GDAL can be tricky to install, consider using conda for this

# For progress bars in processing:
# tqdm>=4.60.0

# For enhanced optimization methods:
# scikit-optimize>=0.9.0

# For faster numerical operations:
# numba>=0.56.0

# For Jupyter notebook support:
# jupyter>=1.0.0
# ipywidgets>=7.6.0
# notebook>=6.4.0

# Development dependencies (only needed for development):
# pytest>=6.0.0
# pytest-cov>=2.12.0
# black>=21.0.0
# flake8>=3.9.0
# mypy>=0.910
# sphinx>=4.0.0
# sphinx-rtd-theme>=0.5.0

# Note: tkinter is required for the GUI but is typically included with Python
# If you encounter tkinter issues, you may need to install it separately:
# - On Ubuntu/Debian: sudo apt-get install python3-tk
# - On CentOS/RHEL: sudo yum install tkinter
# - On macOS: tkinter should be included with Python from python.org
